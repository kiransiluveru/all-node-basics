# all-node-basics
NodeJs Basics Practise


startup command
set NODE_CONFIG={"mail": {"password": "kjsu4#53"}} && nodemon index.js

Each variable needs its own set command separated by &&.

set NODE_CONFIG={"mail": {"password": "kjsu4#53"}} && set DEBUG=app:* && nodemon index.js